//
//~~ _.  Menu items list aka list of links to sub-pages, visualized as images with titles (Links, Images, Buttons) - always visible, since there are only two links, for now
//
//       _._. Single menu item (Link, Image, Button)
//

import styles from "src/styles/sass/styles-all.module.scss";

const MenuItemsLists = () => {
  return <div className={styles["css-class-name"]}>MenuItemsLists</div>;
};
export default MenuItemsLists;
